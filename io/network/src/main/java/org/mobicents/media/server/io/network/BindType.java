package org.mobicents.media.server.io.network;

public enum BindType {
    Default     // Default bind to IP
    , Local     // Bind to Local Address
    , WebRtc    // Bind to WebRTC Address (Interface)
}
